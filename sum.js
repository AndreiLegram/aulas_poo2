function sum(a,b) {
    return a + b;
}

function fatorial() {
    var res;
    if (x == 0) {
        res = 1;
    } else {
        res = x * fatorial(x-1);
    }
    res = x == 0 ? 1 : x * fatorial(x+1)
    return res;
}